#!/usr/bin/env node
// ESM entrypoint preserved for the historic extensionless command name.
// The implementation remains CommonJS in the adjacent .cjs file because it
// uses require(); importing a .cjs file keeps Node on the CJS loader even
// though tools/telegram-provider-router/package.json sets "type": "module".
import "./ht-allowance-watch.cjs";
