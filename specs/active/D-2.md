---
id: D-2
status: locked
class: SUPABASE_CODE_DRAIN
skill: sync-jobs
edit_mode: patch
who: any-agent
auto_go: true
allowed_files:
  - server_routes_sync.ts
  - server_routes_food.ts
  - server_food_catalog.ts
  - server_fdc_resolve.ts
  - server_food_catalog_schema.ts
  - server_food_resolver_curator.ts
  - server_routes_admin.ts
  - src/server/food/brandCurator.ts
  - src/server/food/server_food_db_search.ts
  - src/utils/debugLogRetention.ts
  - src/utils/syncUtils.ts
  - src/utils/supabaseClient.ts
  - src/hooks/useAppSync.ts
  - src/jobs/SupabaseJobSync.ts
  - src/jobs/JobQueueRunner.ts
  - src/jobs/JobStore.ts
  - src/jobs/types.ts
  - src/utils/syncUtils.regression.test.ts
  - src/jobs/__tests__/JobStore.test.ts
  - src/jobs/__tests__/JobSession.contract.test.ts
  - src/jobs/__tests__/SupabaseJobSync.coalesce.test.ts
  - plan/ROADMAP.md
  - plan/DATA_PLANE.md
  - AI_HANDOVER.md
frozen_files:
  - src/App.tsx
  - src/components/LogChat.tsx
  - src/hooks/useAppShellState.ts
  - supabase/migrations/20260913_brand_menu_items_status.sql
  - supabase/migrations/20260917_brand_menu_items_image_url.sql
  - supabase/migrations/20260919_food_logs_source_meal_id.sql
  - plan/R2_DELETE_CANDIDATES.json
  - AGENTS.md
  - docs/agent/standing.json
  - scripts/journey-guard.mjs
  - agents/scoutInstructions.ts
  - agents/dietitianInstructions.ts
gate:
  - npx tsc --noEmit
  - npx vitest run src/utils/syncUtils.regression.test.ts src/jobs/__tests__/JobStore.test.ts src/jobs/__tests__/JobSession.contract.test.ts src/jobs/__tests__/SupabaseJobSync.coalesce.test.ts
  - node scripts/journey-guard.mjs D-2
---

# Packet: D-2 — drain dead Supabase code (keep the project)

Human replies: **go** | stop | one comment. Nothing below is applied yet.

## Journey

Better = `isD1Configured()` is the only SQL path a production request can
take — no `supabaseAdmin` import, no `else if (supabaseAdmin)` fallback that
pays a 402 at runtime, no `Supabase*` names on files that only talk to the
backend — instead of D1-first/Supabase-fallback dual paths scattered over a
dozen files while the remote project stays alive for D-1.

## Findings (do not redo — verified by reading, not guessing)

1. **Client Supabase is already dead by flag.**
   `src/utils/supabaseClient.ts:12` hardcodes `isSupabaseConfigured = false`;
   `syncUtils.ts` / `useAppSync.ts` / `useAuthSession.ts` all gate on it.
2. **Server routes are dual-path: D1 first, Supabase fallback that 402s.**
   `/api/sync/supabase-push` (`server_routes_sync.ts:745`) upserts via
   `d1UpsertFoods`, else falls back to `supabaseAdmin.from('food_logs')`.
   Same shape on `-pull` (`:336`) and deletes. Route NAMES must stay — the
   client regression test POSTs to `/api/sync/supabase-push` — only the
   fallback branches and the import go.
3. **`supabaseAdmin` reference counts** (import + fallback branches):
   `server_routes_sync.ts` 29, `server_food_catalog.ts` 36,
   `server_routes_food.ts` 6, `debugLogRetention.ts` 8,
   `server_fdc_resolve.ts` 2, plus `brandCurator.ts`,
   `server_food_db_search.ts`, `server_routes_admin.ts`,
   `server_food_resolver_curator.ts`, `server_food_catalog_schema.ts`.
   `server_food_catalog.ts` (36) is the suspect for read-order: verify
   catalog reads try D1 before Supabase, and make D1 the only read.
4. **`SupabaseJobSync.ts` is a misnomer, not a live wire.**
   `isDirectClientSupabaseDisabled = true`; it syncs through backend routes
   (`/api/jobs/status`, D1-backed). Rename to `BackendJobSync.ts` and update
   every importer in the SAME commit (L2): `useAppSync.ts`,
   `JobQueueRunner.ts`, and the three `__tests__` files that import from
   `'../SupabaseJobSync'`. `useAppShellState.ts` (imports
   `initSupabaseJobSync`) stays **frozen** — re-export a deprecated alias
   from the new module instead of touching it.
5. **Keep the project and the migrations.** `supabase/migrations/` (3 files,
   incl. `20260919_food_logs_source_meal_id.sql` still unapplied while 402)
   stays frozen until D-1. No dashboard action. Env keys stay (client flag
   file documents the off state).

## Plan

1. **Delete fallback branches, keep route names.**
   In each allowed server file: remove the `supabaseAdmin` import and every
   `else if (supabaseAdmin)` / `if (supabaseAdmin)` fallback so D1 is the
   only path. Do not rename routes, do not change request/response shapes.
   Done when: `rg supabaseAdmin <allowed server files>` is empty and the
   sync regression tests still POST to the same route names green.
2. **Catalog reads D1-only.** Verify `server_food_catalog.ts` read order;
   Supabase becomes no path, not a fallback.
   Done when: no catalog read can emit a 402 (code inspection + existing
   catalog tests green).
3. **Rename the misnomer.** `SupabaseJobSync.ts` → `BackendJobSync.ts`
   (move-only + import updates + deprecated `initSupabaseJobSync` alias for
   the frozen shell state). Class/function behavior unchanged.
   Done when: `rg -l SupabaseJobSync` hits only the alias line, the test
   files, and this packet; the four named test files green.
4. **Client flag file stays.** `supabaseClient.ts` keeps exporting
   `isSupabaseConfigured = false` with a D-1 comment. No client behavior
   change in this packet.

## Test plan

```text
npx tsc --noEmit
npx vitest run src/utils/syncUtils.regression.test.ts src/jobs/__tests__/JobStore.test.ts src/jobs/__tests__/JobSession.contract.test.ts src/jobs/__tests__/SupabaseJobSync.coalesce.test.ts
node scripts/journey-guard.mjs D-2
```

## Audit plan

1. Scope vs ROADMAP: flips the **D-2** row; does not touch D-1, D-9,
   D-10, F-13, or Track V.
2. One frozen example: a push-sync round trip still writes D1 and the
   client still POSTs `/api/sync/supabase-push` (name kept, no 402 emitted).
3. Honest residual named: the remote project + 3 migration files stay until
   D-1; env keys stay; any `supabaseAdmin` hit found outside Allowed is
   reported, not chased.

## Blast radius

Allowed / Frozen are the YAML lists above. `useAppShellState.ts` stays
frozen via the alias — no god-file edit. Out of scope: **deleting the
Supabase project**, applying migrations, paying to unpause, R2 deletes,
renaming the `/api/sync/supabase-*` routes, client sync behavior.

## Stop and come back

Two repairs fail · Frozen file in the diff · A fallback branch turns out
to be the LIVE path (D1 first fails open) · Catalog has rows D1 lacks
(D-1 content — stop, do not backfill) · New class appears.
