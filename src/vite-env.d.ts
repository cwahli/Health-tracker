/// <reference types="vite/client" />

/** Short git commit hash injected by Vite at build / dev-server start. */
declare const __GIT_COMMIT_HASH__: string;
/** ISO 8601 timestamp of the HEAD commit, injected by Vite at build / dev-server start. */
declare const __GIT_COMMIT_TIME__: string;
